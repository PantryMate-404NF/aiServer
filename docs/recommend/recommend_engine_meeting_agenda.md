# 추천 엔진 통합 회의 안건 (에이전트용)

**정하는 것**: 파트 B 추천 엔진을 A(데이터)·C(평가)·백엔드와 합칠 때 정해야 할 안건과 그 진행 상태. 반영된 것은 결정일·커밋·코드를, 결정이 필요한 것은 선택지와 막히는 일을 적습니다

**적용 대상**: 3자 회의 참석자와 파트 B. 사람은 `human/` 의 서술본을 읽습니다

**버전**: 8.0.0 · **최종 수정**: 2026-09-12 · **작성자**: 유재현

---

## 1. 요약

| 키 | 값 |
|---|---|
| 타 파트 안건 (G) | 27건. `결정·반영` 10 · `일부 반영` 4 · `결정 필요` 13 |
| 내부 안건 (N) | 15건. `착수 가능` 4 · `일부 반영` 1 · `선행 대기` 5 · `결정 필요` 3 · `관찰` 2 |
| 가장 급한 것 | G-16 (게이트 예외 4건 Tech Lead 승인). 승인 없이 `main` 에 병합하면 01의 3.3·6.1 위반입니다. 그다음 G-09·G-10 (규약 개정) |
| 근거 | 1차 3자 회의(2026-09-10), `origin/develop-data-part` 병합 실측, 조용한 실패 점검(2026-09-11), 2차 3자 회의(2026-09-11), 3회차 복기(2026-09-12) |
| 갱신 | 상태가 바뀌면 2절 표를 먼저 고치고 해당 항목을 3절 이하의 맞는 자리로 옮깁니다. 되돌리기 어려운 결론은 `docs/decisions/` 에 파일로 남깁니다 |

---

## 2. 상태 표

04의 2.2 규칙 6 이 문서 체크박스의 완료 표시를 금지하므로 **상태 열로 체크리스트를 대신합니다.**

| 상태 | 뜻 | 자세한 자리 |
|---|---|---|
| `결정·반영` | 결정됐고 코드·설정에 반영을 마쳤습니다 | 3.1 |
| `일부 반영` | 선택지 가운데 B 가 할 수 있는 부분만 반영했고 나머지는 결정이 필요합니다 | 3.2, 5절 |
| `결정 필요` | 회의나 담당자의 결정이 있어야 진행합니다 | 4절, 5절 |
| `착수 가능` | 결정은 났고 작업만 남았습니다 | 5절 |
| `선행 대기` | 실 DB·실데이터·타 파트 확인이 먼저입니다 | 5절 |
| `관찰` | 지금 값을 유지하고 실데이터에서 다시 봅니다 | 5절 |

| ID | 안건 | 상태 | 결정일 | 반영 커밋 | 결정자 |
|---|---|---|---|---|---|
| G-01 | 맛 벡터 축 수 | `결정·반영` | 2026-09-10 | `256c702` | A, B |
| G-02 | 스테이지 모델 통일 | `결정·반영` | 2026-09-10 | `c459c2f` · `256c702` | A, B |
| G-03 | 요청·응답 계약 | `결정·반영` | 2026-09-10 | `256c702` · `da6de58` | A, B, 백엔드 |
| G-04 | 같은 경로 파일의 정본 | `결정·반영` | 2026-09-10 | `c459c2f` | A, B |
| G-05 | propensity 의미 | `결정·반영` | 2026-09-10 | `256c702` | A, C |
| G-06 | 후보 조회 계약 | `결정 필요` | | | A, B |
| G-07 | 17 피처 체계 | `결정·반영` | 2026-09-10 | `256c702` | A, B, C |
| G-08 | 단독 스크립트 수집 제외 | `일부 반영` | 2026-09-10 | `da6de58` | A |
| G-09 | 문서 배치 규칙 개정 | `결정 필요` | | | 김민경, 팀 전원 |
| G-10 | 도메인 루트 파일 규약 | `결정 필요` | | | 김민경, A |
| G-11 | 공유 파일 병합 | `결정·반영` | 2026-09-10 | `da6de58` | 병합 담당 |
| G-12 | 결정사항 문서 저장소 반영 | `결정 필요` | | | A |
| G-13 | `pillow-heif` 버전 | `일부 반영` | 2026-09-10 | `da6de58` | A, 영수증 담당 |
| G-14 | Windows 에서 `make` 검사 중단 | `결정 필요` | | | A |
| G-15 | 벤치 스크립트의 로컬 경로 | `일부 반영` | 2026-09-10 | `da6de58` | A |
| G-16 | 게이트 예외 4건 승인 | `결정 필요` | | | 김민경, A, B |
| G-17 | `evaluation/threshold.py` 인수 | `결정 필요` | | | A, C |
| G-18 | `make contract` 조용한 중단 | `일부 반영` | 2026-09-11 | `50d77cf` | A |
| G-19 | `/v1/health` 의 `redis` | `결정 필요` | | | A |
| G-20 | `main` 브랜치 보호 설정 | `결정 필요` | | | 김민경 |
| G-21 | 취향의 원천과 우선순위 | `결정·반영` | 2026-09-11 | `04ec783` | A, B, 기획 |
| G-22 | 시간 감쇠와 주기 가중 | `결정·반영` | 2026-09-11 | `04ec783` | A, B, C |
| G-23 | 사용자 취향 저장소 | `결정·반영` | 2026-09-11 | `04ec783` | A, B |
| G-24 | 3축 척도의 범위 | `결정 필요` | | | A, 기획 |
| G-25 | "월별 주기" 의 해석 | `결정 필요` | | | 기획, B |
| G-26 | `EventIn` 발생 시각 필드 | `결정 필요` | | | A, 백엔드 |
| G-27 | `mixed_exploration` 의 균등 폴백 | `결정 필요` | | | A |
| N-01 | `.env` 정리 | `착수 가능` | | | 유재현 |
| N-02 | 손잡이 정본 | `결정 필요` | | `0da1aa6` · `50d77cf` (임시 방어) | B |
| N-03 | 라우터 실연결 | `선행 대기` | 2026-09-10 | | B |
| N-04 | 요리군 항과 맛 정합의 절충 | `관찰` | | | B |
| N-05 | 탐색·맛 손잡이 값 | `선행 대기` | | | B |
| N-06 | 기준점 태그 | `결정 필요` | | | B |
| N-07 | 결정 기록 분리 | `착수 가능` | | | B |
| N-08 | 계약 검증의 정의 | `착수 가능` | 2026-09-10 | | B |
| N-09 | 17개 특성 보고 | `일부 반영` | 2026-09-11 | `50d77cf` | B |
| N-10 | 난수원과 `rng_seed` | `선행 대기` | | | B |
| N-11 | 사용자 이력의 출처 | `선행 대기` | | | B |
| N-12 | 병합 순서 | `선행 대기` | 2026-09-10 | `da6de58` | A, B |
| N-13 | `f_time_fit` 재정의 | `결정 필요` | | | B, C |
| N-14 | Mock 카탈로그 맛 스케일 | `착수 가능` | | | B |
| N-15 | 완화 종료 기준 상향의 대가 | `관찰` | 2026-09-12 | `b7b25b4` | B |

표에 나오는 커밋입니다.

| 커밋 | 시각 | 요지 |
|---|---|---|
| `c459c2f` | 2026-09-10 17:44 | A 의 스테이지 계약과 엔진 조각 채택 |
| `256c702` | 2026-09-10 17:45 | 6축 맛·17 피처로 점수 계산 재작성. B 의 `schema.py`·`explain`·`penalty`·`feedback` 삭제 |
| `da6de58` | 2026-09-10 18:30 | A 브랜치 전량 병합과 게이트 통과 |
| `0da1aa6` | 2026-09-11 02:08 | 설정값 3종이 실제 계산에 닿게 함 |
| `50d77cf` | 2026-09-11 02:23 | 조용히 통과하던 실패를 드러냄 |
| `d35b619` | 2026-09-11 | 취향 페르소나 결정 기록 |
| `04ec783` | 2026-09-11 | 취향 페르소나 구현 — 고른 음식 우선, 감쇠·주기, JSON 저장소, 취향 없으면 탐색 확대 |

---

## 3. 결정되어 반영된 것

### 3.1 전부 반영 (G)

1차 3자 회의(2026-09-10)의 결정은 전부 **먼저 구현된 A 를 정본으로 한다**는 한 방향입니다. 근거는 `../decisions/2026-09-10_recommend_engine_follows_data_track_contract.md` 에 있습니다. 회의 전의 배경과 선택지는 이 파일의 5.0.0 판(커밋 `86e5c07`)에 남아 있습니다.

| ID | 결정 | 결정일 | 근거 | 반영 커밋 | 반영 코드 |
|---|---|---|---|---|---|
| G-01 | 6축 유지. 사용자에게 받는 앞 3축만 값이 있고 뒤 3축은 None 으로 계산에서 뺍니다. 데이터가 오면 코드 변경 없이 켜집니다 | 2026-09-10 | D-19 | `256c702` | `engine/taste.py` (`FLAVOR_AXES`, `centered_cosine`, `effective_taste`), `engine/context.py` |
| G-02 | A 형태 채택. B 가 `stage.ScoredCandidate` 를 17 피처 dict 로 직접 채웁니다. B 가 제안한 두 층 변환은 채택되지 않았습니다 | 2026-09-10 | D-20 | `c459c2f` · `256c702` | `stage.py`, `engine/feature.py` (`compute_features`), `engine/score.py` |
| G-03 | A 계약 채택. B 의 `schema.py` 를 버리고 엔진은 요청 모델을 보지 않습니다 | 2026-09-10 | D-20 | `256c702` (B 계약 삭제) · `da6de58` (A 계약 유입) | `schema.py` (A), `service.py` (`rank_candidates` 가 요청 대신 문맥을 받음) |
| G-04 | A 정본. B 는 A 의 계약·엔진 조각을 그대로 쓰고 점수 계산만 채웁니다 | 2026-09-10 | D-22 | `c459c2f` | `enums.py`, `stage.py`, `engine/rank.py`, `engine/reason.py`, `engine/explore.py`, `engine/serendipity.py` (A 원본에 표기 수정 26곳만) |
| G-05 | 확률로 통일합니다. 0 < p <= 1 이고 `PROPENSITY_SEMANTICS` 는 "item" 입니다 | 2026-09-10 | D-21 | `256c702` | `engine/rerank.py` (`pick_exploration`, `DETERMINISTIC_PROPENSITY`, `MIN_PROPENSITY`) |
| G-07 | A 의 17 피처를 따릅니다. 못 재는 것은 None 이고 Zero-Drop 이 분모에서 함께 뺍니다 | 2026-09-10 | D-20 | `256c702` | `engine/feature.py`, `engine/score.py` (`weighted_score`) |
| G-11 | 해소. 충돌 8건을 처리했고 `docs/README.md` 는 양쪽 병기, `pyproject.toml` 은 자동 병합 뒤 검사 설정 추가 | 2026-09-10 | D-23~D-26 | `da6de58` | `pyproject.toml`, `docs/README.md`, `tests/conftest.py` |
| G-21 | 6축은 음식의 맛. 취향은 온보딩에서 고른 음식의 6축 평균 → 없으면 3축 척도 → 없으면 없음. 고른 음식이 있으면 척도는 저장만(추후 LLM 추천 보정) | 2026-09-11 | D-29, D-32, D-33 | `04ec783` | `engine/persona.py` (`prior_from_picks`, `prior_from_scales`, `derive_persona`), `service.py` (`onboarding_profile`) |
| G-22 | 레시피 선택 이벤트를 누적해 점진 갱신. 오래된 이벤트는 반감기로 감쇠하고 같은 계절·요일·시간대의 이벤트는 더 무겁게 | 2026-09-11 | D-30 | `04ec783` | `engine/persona.py` (`event_weight`, `time_decay`, `cycle_affinity`, `prune_events`), `policy.py` 손잡이 9개 |
| G-23 | 사용자별 원본을 우리가 관리하는 저장소에. 지금은 JSON, 실 DB 가 붙으면 `user_vector`·`event_log` 로 이전 | 2026-09-11 | D-31 | `04ec783` | `profile_store.py` (`JsonProfileStore`, `load_presented_flavors`), `service.py` (`PersonaService`) |

코드 경로는 `src/features/recommend/` 기준입니다.

### 3.2 일부 반영 (G)

B 가 혼자 할 수 있는 선택지만 먼저 반영했습니다. 나머지는 결정자가 정해야 합니다.

| ID | 반영한 것 | 반영일 | 반영 커밋 | 반영 코드 | 남은 결정 | 결정자 |
|---|---|---|---|---|---|---|
| G-08 | (a) A 의 단독 실행 스크립트 8개를 파일명으로 수집에서 뺐습니다. 글로브가 B 검사 63건과 `integration/test_receipt_pipeline.py` 까지 빼고 있었습니다 (D-26) | 2026-09-10 | `da6de58` | `tests/conftest.py` (`collect_ignore`) | (b) 스크립트를 `scripts/` 로 옮길지. 옮기면 커버리지 측정 범위도 되돌릴 수 있습니다 | A |
| G-13 | `uv.lock` 의 `pillow-heif` 를 `main` 값 1.6.0 으로 되돌렸습니다. 1.7.0 의 DLL 이 Windows 앱 제어 정책에 막혀 영수증 검사 8건이 수집 단계에서 죽었습니다 (D-25, E-12) | 2026-09-10 | `da6de58` | `uv.lock` | 1.7.0 이 필요한 이유가 있는지 확인 | A, 영수증 담당 |
| G-15 | (a) `scripts/reco/bench` 를 검사 대상에서 뺐습니다. 5개 파일이 작성자 PC 의 절대경로를 `exec()` 로 읽어 저장소 안에서 돌지 않습니다 (D-24) | 2026-09-10 | `da6de58` | `pyproject.toml` (`extend-exclude`) | (b) 읽는 파일을 저장소 안으로 옮김, (c) 역할이 끝났으므로 삭제 | A |
| G-18 | (a) 종단 블록 두 곳이 설정 오류를 실패로 셉니다. 빈 `.env` 에서 92통과·2실패·종료코드 1 로 끝까지 돕니다. 전에는 59번째에서 죽고 마지막 줄이 통과 표시였습니다 (F-30) | 2026-09-11 | `50d77cf` | `tests/unit/recommend/test_contract.py` | (c) `Makefile` 이 검사용 환경변수를 세울지 | A |

---

## 4. 결정이 필요한 것 (G)

급한 순서로 적습니다. 모두 상태가 `결정 필요` 입니다.

| ID | 안건 | 배경 | 선택지 | B 의 제안 | 결정자 | 결정 전까지 막히는 것 |
|---|---|---|---|---|---|---|
| G-16 | 게이트 예외 4건의 Tech Lead 승인 | 01의 3.3 은 전역 ignore 에, 6.1 은 커버리지 하한 축소에 승인을 요구합니다. 전역 `ignore = ["RUF001","RUF002","RUF003"]`(A), 파일 단위 예외 10항목·`extend-exclude`·커버리지 `omit` 4항목(B)이 승인 없이 들어 있습니다 | (a) 네 건을 한 번에 승인 요청 (b) 항목별로 판단 (c) 승인 대신 코드를 손으로 고침 | (a). 근거와 해소 조건은 `../decisions/2026-09-10_merge_data_track_gate_exceptions.md` | 김민경, A, B | **`main` 병합.** 승인 없이 병합하면 규칙 위반입니다 |
| G-20 | `main` 브랜치 보호 설정 | 01의 2.1 은 `main` 을 보호 브랜치로 두고 직접 push 와 force push 를 금지합니다(MUST). 2026-09-11 GitHub 공개 API 로 확인하니 `main` 의 `protected` 가 `false` 입니다. 규칙은 있는데 막는 장치가 없어 직접 push 해도 에러가 나지 않습니다 | (a) 보호 규칙을 켬(승인 1명, 3.2 검사 통과를 병합 조건으로) (b) 그대로 두고 규칙으로만 막음 | (a). 규칙만으로는 실수를 막지 못합니다 — 이 저장소에서 에러 없이 틀리는 자리를 여러 번 찾은 것과 같은 이유입니다 | 김민경 | 없습니다. 이번 병합은 PR 로 진행합니다 |
| G-09 | 문서 배치 규칙 개정 | 04의 1.1 트리에 `docs/recommend/` 가 없습니다(P-16). A 는 설계 문서를 저장소 밖에 두고 결정만 올리자고 제안했습니다 | (a) 04 에 `docs/<도메인>/` 를 설계 명세·기록·회의 안건 자리로 추가 (b) 기록은 저장소 밖, 결정만 `decisions/` | (a). 01의 9절 절차. 두 형식 기록의 근거는 `../decisions/2026-09-10_recommend_record_dual_format.md` | 김민경, 팀 전원 | 거부되면 이 폴더의 기록을 저장소 밖으로 옮겨야 합니다 |
| G-10 | 도메인 루트 파일 규약 | 도메인 루트가 9개 파일입니다(P-11, F-34). A 가 `repository.py` 를 나누며(`5d41e8f`) `repository_ingest.py` 가 늘었습니다. 02의 2.2 표에 있는 것은 `__init__`·`router`·`schema`·`service`·`repository`·`tables` 6종이고 `stage.py`·`enums.py`·`policy.py`·`repository_ingest.py` 가 없으며 `tables.py` 는 만들지 않았습니다(DDL 은 `deploy/init/02_schema.sql`). 02의 6절은 새 도메인 파일 5개를 상한으로 둡니다 | (a) 02의 2.2 표에 네 파일을 넣고 6절 상한을 손봄 (b) 모델을 `schema.py` 로 되돌림 | (a). A 공유 문서 2절과 D-17 | 김민경, A | 규약과 어긋난 채 `main` 에 들어갑니다. `repository_ingest.py` 가 629줄로 02의 5.1 상한 500줄을 넘고, 루트 파일 9개가 같은 절의 디렉터리 상한 8개를 넘는 것도 이 논의에 묶입니다 |
| G-06 | 후보 조회 계약 | A 에 `repository.retrieve` 와 SQL 함수(A 의 D-14)가 있습니다. B 는 넘겨받은 풀 안에서 부족 재료 k 를 2→3→4 로 완화합니다(A-10). A 의 LIMIT 과 k 처리에 따라 폴백이 빈손이 될 수 있습니다 | (a) A 함수가 k=4 로 넉넉히 반환하고 B 가 안에서 완화 (b) B 가 단계마다 재호출 (c) 완화 로직을 SQL 안으로 | (a). 정렬이 부족수 오름차순이라 상위 500 이 k=2 후보를 포함합니다. 인기순 폴백은 별도 호출 | A, B | 서빙 개통(DB 전환 점검표의 라우터·후보 조회 항목) |
| G-12 | 결정사항 문서 저장소 반영 | A 의 `05_작업분담_결정사항.md`(D-1~D-17)가 로컬에만 있어 축 순서 사고가 났습니다 | (a) `docs/decisions/` 형식으로 나눠 올림 (b) 통째로 올림 | (a). 04의 1.2 파일명 규칙. B 도 같은 형식으로 올립니다(N-07) | A | 두 파트의 결정이 같은 형식으로 남지 않습니다 |
| G-14 | Windows 에서 `make` 검사 중단 | A 의 검사 스크립트가 통과 표시(U+2713)를 출력하는데 Windows 기본 콘솔이 cp949 라 `UnicodeEncodeError` 로 끝납니다. 검사 실패가 아니라 출력 실패인데 종료 코드가 1 입니다(E-11) | (a) `Makefile` 의 `PY` 에 `PYTHONIOENCODING=utf-8` (b) 스크립트에서 `sys.stdout.reconfigure` (c) 각자 환경변수 | (a). 한 곳에서 끝납니다 | A | Windows 에서 `make data-gate` 를 그대로 쓸 수 없습니다 |
| G-17 | `evaluation/threshold.py` 인수 | 57문 전부 0% 이고 저장소 안에서 import 하는 코드가 없습니다. 평가 파트가 쓸 유틸로 보입니다 | (a) C 가 인수하고 검사를 붙임 (b) 쓰는 곳이 생길 때까지 그대로 (c) 삭제 | (a). 커버리지에서 일부러 빼지 않아 0% 가 계속 보입니다 | A, C | 없습니다 |
| G-19 | `/v1/health` 의 `redis` | 저장소 어디에도 redis 클라이언트가 없는데 `HealthOut.redis` 기본값이 True 라 항상 참입니다(F-31). 같은 응답의 `db` 는 `0da1aa6` 에서 실제 확인으로 고쳤습니다(F-24) | (a) 필드를 뺌 (b) redis 를 붙이고 확인 (c) 그대로 둠 | (a). 근거 없는 참은 없느니만 못합니다 | A | 헬스체크가 근거 없는 참을 계속 냅니다 |
| G-24 | 3축 척도의 범위 | 데이터 파트 계약(`OnboardingIn.scales`)은 0~4 이고 2차 회의 기록은 1~5 입니다. 엔진은 0~1 로 정규화된 값만 받으므로 변환은 `service.onboarding_profile()` 한 곳(`SCALE_MAX`)에 있습니다 | (a) 계약대로 0~4 (b) 회의대로 1~5 로 계약 변경 | (a). 계약 검증 98건이 0~4 를 전제합니다. 프론트가 1~5 로 보이더라도 서버에 0~4 로 보내면 됩니다 | A, 기획 | 값이 어긋나면 척도 사용자의 취향이 한 단계씩 밀립니다. 에러는 나지 않습니다 |
| G-25 | "월별 주기" 의 해석 | 2차 회의는 "계절·월·일 주기" 를 말했습니다. B 는 월별을 연 주기의 위상(달이 다르면 위상이 다름)으로 읽어 연·주·일 세 주기만 두었고 월초·월말 같은 월내 주기는 두지 않았습니다. 결정 기록에 이 해석이 적혀 있지 않았습니다 | (a) 연 주기 위상으로 충분 (b) 월내 주기 손잡이 추가 | (a). 취향이 월초·월말에 달라진다는 근거가 없습니다. 필요해지면 `cycle_affinity` 위에 손잡이 하나입니다 | 기획, B | 없습니다. 해석이 틀렸으면 손잡이 하나를 더합니다 |
| G-26 | `EventIn` 발생 시각 필드 | `EventIn` 에 시각이 없어 이벤트 시각은 서버 수신 시각입니다(결정 기록 5절). 오프라인 동기화로 200건이 한 번에 오면 전부 같은 시각을 받아 감쇠와 주·일 주기가 동기화 시각으로 재어집니다 | (a) 선택 항목 `occurred_at`(시간대 있는 datetime)을 계약에 더하고 없으면 수신 시각 (b) 수신 시각으로 충분 | (a). 주·일 주기(`weekly_cycle_strength`·`daily_cycle_strength`)를 켤 조건입니다. 시간대 없는 시각은 엔진이 이미 거부합니다 | A, 백엔드 | 주·일 주기를 켜지 못합니다(지금 0). 연 주기와 감쇠에는 하루 이내 오차라 영향이 작습니다 |
| G-27 | `mixed_exploration` 의 균등 폴백 | `Candidate.cluster_id` 의 계약은 "None 이면 균등 탐색으로 폴백" 인데 A 의 `serendipity.mixed_exploration` 은 군집이 없으면 Thompson 몫을 비웁니다(F-42). 클러스터링 배치가 돌기 전까지 탐색 절반이 예외 없이 사라집니다. B 는 `rerank.effective_uniform_share()` 로 밖에서 우회하고 추적에 `explore_fallback` 을 남깁니다(D-35, `b7b25b4`) | (a) A 가 함수 안에서 군집이 없으면 균등 몫을 k 로 (b) B 우회 유지 | (a). 계약이 있는 자리에서 구현하는 것이 맞고, 그러면 B 의 우회를 지웁니다 | A | 없습니다. 우회가 동작합니다 |

---

## 5. B 안에서 처리할 것 (N)

| ID | 안건 | 상태 | 처리 | 막히는 것 · 다음 행동 | 반영 |
|---|---|---|---|---|---|
| N-01 | `.env` 정리 | `착수 가능` | 필수 6개만 채우고 기본값이 있는 15줄을 지웁니다(P-01). 값은 팀 비밀 저장소에 있어 유재현이 넣습니다 | 비어 있으면 서버 기동과 `make contract` 가 실패합니다(E-13) | |
| N-02 | 손잡이 정본 | `결정 필요` | `Settings` 와 `RankingPolicy` 중 어느 쪽을 정본으로 할지 정합니다(03의 2절, D-04). `.env.example` 갱신 동반 | 정하기 전까지 `.env` 로 값을 바꿔도 엔진에 닿지 않습니다(F-32) | 임시 방어 — `0da1aa6` 이 `propensity_mc` 를 실제 계산에 넘기고(F-25), `50d77cf` 가 두 값이 같음을 검사로 붙잡습니다 |
| N-03 | 라우터 실연결 | `선행 대기` | G-04 결정(2026-09-10)으로 A 의 `router.py` 에 `service.rank_candidates` 를 끼웁니다 | 실 DB. 후보 조회·로그 적재·난수 시드와 같은 변경에서 처리합니다 | |
| N-04 | 요리군 항과 맛 정합의 절충 | `관찰` | 양식 선호 단맛 사용자에서 맛 lift 가 -0.04 입니다(F-13). `f_cuisine` 0.04 와 `f_taste` 0.16 이 각자 항이라 절충이 가중치로 드러납니다 | 실데이터 재측정 뒤 W3 학습으로 넘깁니다 | |
| N-05 | 탐색·맛 손잡이 값 | `선행 대기` | `policy.RankingPolicy` 의 값 전부(예시값, 실제 데이터로 대체 필요). `uniform_share` 0.5 와 `propensity_mc` 200 은 A 설계를 따랐고 나머지는 Mock 기준 | 실데이터 후보 500건. 지문에 들어가므로 바꿀 때마다 기록합니다 | |
| N-06 | 기준점 태그 | `결정 필요` | Layer 1 완료 `cbdf312`, 수정 반영 `7d72324` 에 `reco-b-layer1` 태그를 붙일지 | 없습니다 | |
| N-07 | 결정 기록 분리 | `착수 가능` | D-01(개인화 선행)·D-16(축 순서)·D-17(`stage.py`)을 `YYYY-MM-DD_제목.md` 로 씁니다 | G-12 가 정해지면 형식을 맞춥니다 | |
| N-08 | 계약 검증의 정의 | `착수 가능` | G-03 결정(2026-09-10)으로 B 의 계약 검사를 A 의 `test_contract.py`(98건)에 편입합니다(T-12) | 없습니다 | |
| N-09 | 17개 특성 보고 | `일부 반영` | 평가 스크립트가 매 실행마다 값이 변하는 피처 수와 순위를 못 바꾸는 가중치 합을 냅니다. 지금 재는 것은 9종이고 0.26 이 순위를 못 바꿉니다(F-28, T-11) | 보고서 자체는 아직 없습니다. 사용자 이력이 붙은 뒤 다시 잽니다 | `50d77cf` — `scripts/eval_recommend_mock.py` 의 `report_dead_weight` |
| N-10 | 난수원과 `rng_seed` | `선행 대기` | 서빙에서 `random.Random(rng_seed)` 를 만들어 넘깁니다(F-16) | N-03 과 같은 변경에서 처리합니다 | |
| N-11 | 사용자 이력의 출처 | `선행 대기` | `user_ingredient_pref`·`event_log`·`user_cluster_stat` 을 읽는 저장소 함수를 만듭니다(F-15) | 실 DB. 그 전까지 가중치 0.21 이 순위에 관여하지 못합니다 | |
| N-12 | 병합 순서 | `선행 대기` | B 가 먼저 `main` 에 병합하고 A 가 `main` 을 받아 자기 수정을 얹기로 2026-09-10 에 정했습니다 | A 개발자에게 A 파일 37개의 차이를 먼저 보입니다. 대부분 포맷터 출력이고 의미 변경은 D-23 뿐입니다 | `da6de58` |
| N-13 | `f_time_fit` 재정의 | `결정 필요` | 후보 조회가 조리시간 상한을 이미 걸러 살아남은 후보가 전부 1.0 입니다(F-27). 여유분을 재는 쪽으로 바꾸면 순위가 달라집니다 | W3 쌍대비교 학습(N-04·N-05)에서 `f_pantry_use`·`f_quality` 가중치와 함께 정합니다 | |
| N-14 | Mock 카탈로그 맛 스케일 | `착수 가능` | 생성기의 맛 값(0.2~0.75)이 시드의 실집계(0~0.6)와 분포가 달라, 고른 음식으로 만든 취향이 Mock 코퍼스 평균 아래에 놓입니다(F-35). 맛 정합 lift 가 작게 나옵니다 | 생성기의 요리군별 기준 벡터를 시드 분포에 맞춥니다. 실데이터에는 없는 문제라 급하지 않습니다 | |
| N-15 | 완화 종료 기준 상향의 대가 | `관찰` | 완화 조회 종료 기준을 24건에서 36건(취향 없으면 52건)으로 올려 탐색 칸이 전부 채워졌습니다(D-34, F-41). 대신 Mock 카탈로그 120건에서 인기순 폴백이 12명 중 7명으로 늘었습니다 | 실데이터(후보 수백 건)에서 폴백 비율을 다시 잽니다. 부족하면 `exploration_min_pool_ratio` 를 낮추는 것이 먼저이고 기준을 되돌리는 것은 마지막입니다 | `b7b25b4` — `engine/candidate.py` `needed()` |

실 DB 가 필요한 항목의 세부 절차는 `recommend_engine_db_cutover.md` 에 있습니다.
